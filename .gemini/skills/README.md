# SharpPragma Gemini skills

Install the `sharp-*` folders here by running:

```bash
bash scripts/install-gemini-skills.sh
```

This directory is included as a convenience placeholder. The source of truth is the repository root skill folders.
